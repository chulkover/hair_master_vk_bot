# Зависимости бота. Ставятся так:
#   .venv/bin/pip install -r requirements.txt
#
# Версии зафиксированы теми, что были в окружении на Windows-ПК, — чтобы
# после переустановки поведение не поехало. vk_api и все его зависимости
# здесь на чистом Python, компилятор для установки не нужен.
vk_api==11.10.0
requests==2.32.5
