# секреты — токен НИКОГДА не должен попадать в git
token.txt
tg_token.txt

# рабочие заметки для Claude Code — в git не едут
CLAUDE.md

# виртуальное окружение и мусор python
.venv/
__pycache__/
*.pyc

# настройки PyCharm (можно и оставить, но обычно их не коммитят)
.idea/

# индекс codegraph — локальный, пересобирается командой codegraph init
.codegraph/

# данные бота
schedule.txt
subscribers.txt
*.db
*.db-wal
*.db-shm

# копии данных, сделанные перед переездом в базу
*.bak
*.bak-*
